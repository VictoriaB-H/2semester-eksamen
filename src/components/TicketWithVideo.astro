<div class="ticket">
  <!-- Overskrift -->
  <header class="ticket-overskrift">
    <h3>AV, MIN GULDTAND</h3>
  </header>

  <!-- Lokation og priser -->
  <section class="ticket-info">
    <div class="location">
      <strong>BIRKEPLADSEN</strong><br />
      TISVILDELEJE
    </div>
    <div class="price">Fredag: 375 DKK</div>
    <div class="price">Lørdag: 375 DKK</div>
    <div class="price">Partout: 500 DKK</div>
  </section>

  <!-- Visuel separator -->
  <div class="ticket-separator"></div>

  <!-- Beskrivelse -->
  <section class="ticket-desc">
    Køb dine billetter i dag, og støt op om kulturlivet i Tisvildeleje. Vi
    glæder os til at se dig!
  </section>

  <!-- Illustrativt billede -->
  <video autoplay muted loop playsinline class="ticket-image">
    <source src="../img/testvideo.mov" type="video/mp4" />
  </video>

  <div class="ticket-button">
    <a
      href="https://www.ticketmaster.dk/artist/av-min-guldtand-billetter/1050692"
      target="_blank"
      rel="noopener noreferrer"
    >
      <span>KØB BILLET</span>
    </a>
  </div>

  <!-- Dato -->
  <div class="ticket-dates">30.05.25 – 31.05.25</div>

  <!-- Separator mellem indhold og stregkode -->
  <div class="date-separator"></div>

  <!-- Stregkode til scanning -->
  <div class="barcode">
    <img src="/img/stregkode.png" alt="Stregkode til billet" />
  </div>
</div>

<style>
  .ticket {
    max-width: 320px;
    margin: auto;
    background: white;
    border-radius: 6px;
    padding: 0;
    overflow: hidden;
    clip-path: polygon(
      10px 0%,
      /* top left indhak */ calc(100% - 10px) 0%,
      /* top right indhak */ 100% 10px,
      /* top right indhak */ 100% calc(100% - 10px),
      /* bottom right indhak */ calc(100% - 10px) 100%,
      /* bottom right indhak */ 10px 100%,
      /* bottom left indhak */ 0% calc(100% - 10px),
      /* bottom left indhak */ 0% 10px /* top left indhak */
    );
  }
  .ticket-overskrift {
    padding: 10px;
    text-align: center;
  }
  .ticket-overskrift h3 {
    color: #cf3237;
    font-size: 2.5rem;
    margin: 0 0 12px;
    font-weight: bold;
  }
  .ticket-info {
    padding: 0 16px;
    font-size: 16px;
    line-height: 1.1;
    display: flex;
    flex-direction: column;
    gap: 2px;
    color: #000;
  }
  .ticket-info .location {
    text-align: right;
    font-weight: bold;
    color: #000;
    font-family: "Anton", sans-serif;
  }
  .ticket-separator {
    border-top: 1px solid #000;
    margin: 12px 0px;
  }
  .ticket-desc {
    padding: 0px 16px;
    font-size: 15px;
    font-weight: bold;
    color: #000;
    font-family: "Anton", sans-serif;
  }
  .ticket-image {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    display: block;
  }
  .ticket-button {
    text-align: center;
  }

  .ticket-button a {
    background: #000;
    color: white;
    font-weight: bold;
    font-size: 20px;
    text-decoration: none;
    border: none;
    padding: 12px 28px;
    cursor: pointer;
    display: inline-block;
    margin-top: 10px;
    transition: transform 0.2s ease-in-out;
    font-family: "Anton", sans-serif;
  }

  .ticket-button a span {
    display: inline-block;
    transition: transform 0.2s ease-in-out;
  }

  .ticket-button a:hover span {
    transform: scale(1.1);
  }
  .ticket-dates {
    text-align: center;
    color: #d42c2c;
    font-size: 2rem;
    font-family: owners-text, sans-serif;
    padding: 10px;
  }
  .date-separator {
    border-top: 1px solid #000;
    margin: 10px 0;
  }
  .barcode {
    padding: 0 0 16px 0;
    text-align: center;
  }
  .barcode img {
    width: 95%;
    height: auto;
    text-align: center;
  }
  .price {
    font-family: "Anton", sans-serif;
  }
  @media screen and (min-width: 768px) {
    .ticket {
      display: grid;
      grid-template-columns: 3fr 4fr 1fr;
      grid-template-rows: auto 1fr auto;
      max-width: 1000px;
      margin: auto;
      padding: 10px;
      background-color: white;
      border: 6px solid black;
      clip-path: polygon(
        20px 0%,
        calc(100% - 20px) 0%,
        100% 20px,
        100% calc(100% - 20px),
        calc(100% - 20px) 100%,
        20px 100%,
        0% calc(100% - 20px),
        0% 20px
      );
    }

    .ticket-desc {
      grid-column: 1;
      grid-row: 1;
      font-size: 18px;
      font-weight: bold;
      padding: 10px 20px;
    }

    .ticket-image {
      grid-column: 1;
      grid-row: 2 / span 2;
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .ticket-overskrift {
      grid-column: 2;
      grid-row: 1;
      align-self: start;
      padding: 10px 0;
    }

    .ticket-overskrift h3 {
      font-size: 57px;
      color: #cf3237;
      font-weight: 900;
      margin: 0;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .ticket-info {
      grid-column: 2;
      grid-row: 2;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      padding: 0 0 0 10px;
      font-size: 20px;
    }

    .ticket-info .location {
      margin-top: 20px;
      font-weight: bold;
      text-align: right;
      text-transform: uppercase;
    }
    .ticket-button {
      grid-column: 2;
      grid-row: 2;
      justify-self: start;
      align-self: start;
      margin-left: 10px;
      margin-top: 165px;
    }
    .ticket-button a {
      font-size: 18px;
      padding: 10px 20px;
      cursor: pointer;
      display: inline-block;
    }
    .ticket-dates {
      grid-column: 3;
      grid-row: 1 / span 2;
      writing-mode: vertical-rl;
      text-align: center;
      font-size: 25px;
      color: #cf3237;
      position: relative;
      top: 20px;
      left: -100px;
      letter-spacing: 0.2rem;
      line-height: 2;
      font-family: owners-text, sans-serif;
      justify-self: center;
    }

    .barcode {
      grid-column: 3;
      grid-row: 3;
      display: flex;
      padding-left: 10px;

      transform: rotate(90deg);
      justify-content: center;
      align-items: center;
    }

    .barcode img {
      width: 300px;
      height: auto;
      position: relative;
      top: 50px;
      left: -170px;
    }

    .ticket-separator,
    .date-separator {
      display: none;
    }
  }
</style>
