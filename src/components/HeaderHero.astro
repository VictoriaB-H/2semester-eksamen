<div class="container">
  <video autoplay muted loop playsinline class="mobilvideo">
    <source src="../img/mobilvideo.mov" type="video/mp4" />
  </video>
</div>

<video autoplay muted loop playsinline class="desktopvideo">
  <source src="../img/hero.mov" type="video/mp4" />
</video>

<div class="main-content">
  <h1>AV,&nbsp;MIN&nbsp;GULDTAND</h1>
  <img class="stjernen" src="/img/stjernen.png" alt="" />
  <a href="/tickets" class="btn">BILLETTER</a>
</div>

<div class="banner-wrapper">
  <div class="banner">
    <img src="../img/banner.png" alt="Banner" class="banner-img" />
    <img src="../img/banner.png" alt="Banner Duplicate" class="banner-img" />
  </div>
</div>

<style>
  h1 {
    margin-top: 20px;
    margin-bottom: 5px;
  }
  .container {
    width: 100vw;
    margin: 0;
    padding: 0;
    position: relative;
    height: 100vh;
    overflow: hidden;
    margin-top: -70px;
    margin-left: -35px;
  }

  .mobilvideo,
  .desktopvideo {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .desktopvideo {
    display: none;
  }

  .main-content {
    position: absolute;
    top: 35%; /* Ændret fra 50% til 35% */
    left: 50%;
    transform: translateX(-50%); /* Kun horisontal centrerings-transform */
    text-align: center;
    z-index: 2;
    color: black;
  }
  .btn {
    display: inline-block;
    padding: 0.1rem 0.5rem;
    border: 2px solid black;
    text-decoration: none;
    color: black;
    background-color: transparent;
    transition:
      border-color 0.3s,
      color 0.3s;
    font-family: "Anton", sans-serif;
    font-size: 25px;
  }

  .btn:hover {
    color: #fffa3c;
    border-color: #fffa3c;
  }

  .banner-wrapper {
    width: 100vw;
    margin: 0;
    padding: 0;
    overflow: hidden;
    background-color: #ece8dd;
    position: relative;
    left: 50%;
    right: 50%;
    transform: translateX(-50%);
  }

  .banner {
    display: flex;
    width: fit-content;
    animation: scroll-left 25s linear infinite;
  }

  .banner {
    display: flex;
    width: fit-content;
    animation: scroll-left 25s linear infinite;
  }

  .banner-img {
    display: block;
    height: auto;
    max-height: 100px;
    flex-shrink: 0;
  }
  .stjernen {
    display: none;
  }
  */ @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  /* DESKTOP (fra 768px og op) */
  @media screen and (min-width: 768px) {
    .mobilvideo {
      display: none;
    }

    .desktopvideo {
      display: block;
    }

    h1 {
      margin-top: 20px;
    }
    .btn {
      font-size: 30px;
      padding: 0.5rem 2rem;
      top: 30%;
    }

    .stjernen {
      position: absolute;
      left: 750px;
      top: -65px;
      display: block;
    }
    .banner-wrapper {
      margin-top: 30px;
    }
  }
  @media screen and (min-width: 768px) and (max-width: 1024px) {
    .stjernen {
      position: absolute;
      width: 130px;
      left: 635px; /* eller et andet passende tal */
      top: -70px;
      display: block;
    }
  }
</style>
