<div class="container">
  <video autoplay muted loop playsinline class="mobilvideo">
    <source src="../img/mobilvideo.mov" type="video/mp4" />
  </video>

  <video autoplay muted loop playsinline class="desktopvideo">
    <source src="../img/hero.mov" type="video/mp4" />
  </video>

  <main class="main-content">
    <h1>AV, MIN GULDTAND</h1>
    <a href="#" class="btn">BILLETTER</a>
  </main>
</div>

<div class="banner">
  <img src="../img/banner.png" alt="Banner" class="banner-img" />
  <img
    src="../img/banner.png"
    alt="Tisvilde Banner Duplicate"
    class="banner-img"
  />
</div>

<style>
  .container {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
    margin-top: -50px;
  }

  .mobilvideo,
  .desktopvideo {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1;
  }

  .desktopvideo {
    display: none;
  }

  .main-content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    z-index: 2;
    color: black;
  }

  .main-content h1 {
    font-size: 1.5rem;
    font-weight: bold;
    margin-bottom: 1rem;
  }

  .btn {
    display: inline-block;
    padding: 0.5rem 1rem;
    border: 2px solid black;
    text-decoration: none;
    font-weight: bold;
    color: black;
    background-color: transparent;
    transition:
      border-color 0.3s,
      color 0.3s;
  }

  .btn:hover {
    color: #fffa3c;
    border-color: #fffa3c;
  }

  .banner {
    display: flex;
    width: fit-content;
    animation: scroll-left 25s linear infinite;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  /* DESKTOP (fra 768px og op) */
  @media screen and (min-width: 768px) {
    .mobilvideo {
      display: none;
    }

    .desktopvideo {
      display: block;
    }

    .main-content h1 {
      font-size: 3rem;
    }

    .btn {
      font-size: 1.25rem;
      padding: 0.75rem 1.5rem;
    }
  }
</style>
