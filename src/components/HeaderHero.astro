<div class="container">
  <video autoplay muted loop playsinline class="mobilvideo">
    <source src="../img/mobilvideo.mov" type="video/mp4" />
  </video>
</div>

<video autoplay muted loop playsinline class="desktopvideo">
  <source src="../img/hero.mov" type="video/mp4" />
</video>

<div class="main-content">
  <h1>AV,&nbsp;MIN&nbsp;GULDTAND</h1>
  <img class="stjernen" src="/img/stjerne.webp" alt="" />
  <a href="/tickets" class="btn">BILLETTER</a>
</div>

<div class="banner-wrapper">
  <div class="banner">
    <img src="../img/banner.webp" alt="Banner" class="banner-img" />
    <img src="../img/banner.webp" alt="Banner Duplicate" class="banner-img" />
  </div>
</div>

<style>
  h1 {
    margin-top: 20px;
    margin-bottom: 5px;
  }
  .container {
    width: 100vw;
    height: 100vh;
    margin-top: -70px;
    margin-left: -35px;
  }

  .mobilvideo,
  .desktopvideo {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .desktopvideo {
    display: none;
  }

  .main-content {
    position: absolute;
    top: 35%;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    color: black;
  }

  .btn {
    padding: 0.1rem 0.5rem;
    border: 2px solid black;
    text-decoration: none;
    color: black;
    font-family: "Anton", sans-serif;
    font-size: 25px;
  }

  .btn:hover {
    color: #fffa3c;
    border-color: #fffa3c;
  }

  .banner-wrapper {
    width: 100vw;
    margin: 10px 0;
    overflow: hidden;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
  }

  .banner {
    display: flex;
    width: fit-content;
    animation: scroll-left 25s linear infinite;
  }

  .stjernen {
    display: none;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  /* DESKTOP */
  @media screen and (min-width: 768px) {
    .mobilvideo {
      display: none;
    }

    .desktopvideo {
      display: block;
    }

    .btn {
      font-size: 30px;
      padding: 0.5rem 2rem;
    }

    .stjernen {
      position: absolute;
      left: 780px;
      top: -65px;
      display: block;
      width: 15%;
    }
  }
  @media screen and (min-width: 768px) and (max-width: 1024px) {
    .stjernen {
      position: absolute;
      left: 680px;
      top: -50px;
    }
  }

  @media screen and (min-width: 1024px) {
    .container,
    .banner-wrapper {
      width: 100vw;
      left: 50%;
      transform: translateX(-50%);
    }
  }
</style>
