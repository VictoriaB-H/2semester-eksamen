<div class="banner-wrapper">
  <div class="banner">
    <img src="../img/bildebanner.webp" alt="Banner" class="banner-img" />
    <img
      src="../img/bildebanner.webp"
      alt="Banner Duplicate"
      class="banner-img"
    />
  </div>
</div>

<style>
  .banner-wrapper {
    width: 100vw;
    overflow: hidden;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
  }

  .banner {
    display: flex;
    width: fit-content;
    animation: scroll-left 25s linear infinite;
  }

  .banner-img {
    max-height: 150px;
    margin-top: 50px;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @media (min-width: 768px) {
    .banner-img {
      max-height: 210px;
      margin-top: 30px;
    }
  }
</style>
