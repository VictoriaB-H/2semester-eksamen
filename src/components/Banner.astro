<div class="bildebanner-wrapper">
  <div class="bildebanner">
    <div class="bildebanner-track">
      <img src="../img/bildebanner.webp" alt="" />
      <img src="../img/bildebanner.webp" alt="" />
    </div>
  </div>
</div>

<style>
  .bildebanner-wrapper {
    width: 100vw;
    overflow: hidden;
    position: relative;
    margin: -35px;
    margin-top: 40px;
  }

  .bildebanner {
    display: flex;
    width: max-content;
    animation: scroll-left 25s linear infinite;
  }

  .bildebanner-track {
    display: flex;
  }

  .bildebanner img {
    display: block;
    height: auto;
    max-height: 180px;
    flex-shrink: 0;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(0%);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @media (min-width: 768px) {
    .bildebanner img {
      max-height: 280px;
    }
    .bildebanner-wrapper {
      width: 100vw;
      overflow: hidden;
      position: relative;
      margin-left: calc(-1 * var(--global-margin-left, 35px));
      margin-right: 0; /* sikrer ikke at højre margin bliver negativ */
      margin-top: 40px;
    }
  }
</style>
