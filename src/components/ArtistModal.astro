---
const { artist, isOpen, onClose } = Astro.props;
---

{
  isOpen && (
    <div class="overlay" onClick={onClose}>
      <div class="modal" onClick={(e) => e.stopPropagation()}>
        <button class="close" onClick={onClose}>
          ×
        </button>
        <img src={"/img/" + artist.img} alt={artist.name} />
        <h3>{artist.name}</h3>
        <div class="time">
          <span>{artist.day}</span>
          <span>{artist.time}</span>
        </div>
        <p>{artist.description}</p>
        <iframe
          src={artist.spotify_embed_url}
          width="100%"
          height="80"
          frameborder="0"
          allowtransparency="true"
          allow="encrypted-media"
        />
        <p>🔊 Lyt her</p>
      </div>
    </div>
  )
}

<style>
  .overlay {
    position: fixed;
    top: 0;
    right: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6);
    display: flex;
    justify-content: flex-end;
    z-index: 1000;
    animation: fadeIn 0.3s ease-out;
  }

  .modal {
    background: black;
    color: white;
    width: 90%;
    max-width: 400px;
    height: 100%;
    padding: 1rem;
    overflow-y: auto;
    animation: slideIn 0.4s ease-out;
  }

  .close {
    font-size: 2rem;
    background: none;
    border: none;
    color: white;
    float: right;
    cursor: pointer;
  }

  .time {
    display: flex;
    justify-content: space-between;
    margin: 1rem 0;
    border: 1px solid white;
    padding: 0.5rem;
  }

  @keyframes slideIn {
    from {
      transform: translateX(100%);
    }
    to {
      transform: translateX(0%);
    }
  }

  @keyframes fadeIn {
    from {
      background: rgba(0, 0, 0, 0);
    }
    to {
      background: rgba(0, 0, 0, 0.6);
    }
  }
</style>
